---
- name: Register an incident on SN
  hosts: all
  tasks:
  - name: Create a incident
    snow_record:
      state: present
      username: admin
      password: cbOheOVW8pA1
      instance: dev110714
      data:
        severity: 3
        priority: 2
        short_description: "Apache failed to install"
        description: "Apache failed to install on {{ ansible_all_ipv4_addresses }}"
        category: Software
    register: new_incident
    delegate_to: Synchronise AWS inventory

  - debug: 
      var: new_incident.record
