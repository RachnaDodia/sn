---
- name: Register an incident on SN
  hosts: localhost
  connection: local
  gather_facts: no
  tasks:
  - name: Create a incident
    snow_record:
      state: present
      table: incident
      username: admin
      password: GKqcKy3D7eqF
      instance: dev60028
      data:
        severity: 2
        priority: 2
        short_description: "New VM created sucessfully on Azure"
        caller: "Rachna Dodia"
        category: Network
    register: new_incident

  - debug: 
      var: new_incident.record
