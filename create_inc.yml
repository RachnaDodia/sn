---
- name: Register an incident on SN
  hosts: localhost
  connection: local
  gather_facts: no
  tasks:
  - name: Create a incident
    snow_record:
      state: present
      username: admin
      password: cbOheOVW8pA1
      instance: dev110714
      data:
        severity: 3
        priority: 2
        short_description: "Server successfully created and Apache configured"
        description: "Access Web Application on {{ ip }}"
        category: Software
    register: new_incident

  - debug: 
      var: new_incident.record
